# XeusGUI

Графический интерфейс для управления Modbus устройствами. Приложение позволяет контролировать различные параметры оборудования через протокол Modbus RTU, включая управление вентиляторами, реле, клапанами, установку температурных и других параметров.

## 📋 Содержание

- [Описание](#описание)
- [Требования](#требования)
- [Быстрый старт](#быстрый-старт)
- [Установка](#установка)
- [Запуск приложения](#запуск-приложения)
- [Сборка приложения](#сборка-приложения)
- [Структура проекта](#структура-проекта)
- [Устранение проблем](#устранение-проблем)
- [Разработка](#разработка)

## 📖 Описание

XeusGUI - это кроссплатформенное приложение на базе PySide6 (Qt для Python) и QML, предназначенное для управления Modbus устройствами. Приложение предоставляет интуитивный графический интерфейс для:

- Управления вентиляторами (впускные, выпускные, опклет, лазер)
- Управления реле (Laser PSU, Magnet PSU, PID Controller, Water Chiller, Vacuum Pump, Vacuum Gauge)
- Управления клапанами
- Установки температурных параметров (Water Chiller, Magnet PSU, Laser PSU, Seop Cell)
- Установки давления (Xenon, N2)
- Мониторинга состояния устройств в реальном времени

## 🔧 Требования

### Системные требования

**macOS:**
- macOS 10.13 или новее
- Python 3.8+ (рекомендуется 3.10 или 3.11)
- 500 MB свободного места на диске

**Windows:**
- Windows 10/11 (64-bit)
- Python 3.8+ (рекомендуется 3.10 или 3.11)
- 500 MB свободного места на диске

### Программные зависимости

- **Python 3.8+** - интерпретатор Python
- **PySide6** (>=6.5.0) - Qt для Python
- **pymodbus** (>=3.5.0) - библиотека для работы с Modbus
- **PyInstaller** (>=5.0) - для сборки исполняемых файлов (опционально)

### Дополнительные требования для сборки

- **PyInstaller** - устанавливается автоматически при сборке
- **Git** - для клонирования репозитория (опционально)

## 🚀 Быстрый старт

### Вариант 1: Запуск готового приложения

**macOS:**
1. Скачайте `XeusGUI.app` из [релизов](https://github.com/pebenifi/xeus/releases)
2. Распакуйте архив
3. Двойной клик на `XeusGUI.app` для запуска

**Windows:**
1. Скачайте `XeusGUI-windows-onedir.zip` из [релизов](https://github.com/pebenifi/xeus/releases)
2. Распакуйте архив
3. Запустите `XeusGUI.exe` из папки `XeusGUI`

### Вариант 2: Запуск из исходного кода

```bash
# Клонируйте репозиторий
git clone https://github.com/pebenifi/xeus.git
cd xeus

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
# macOS/Linux:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Запустите приложение
python main.py
```

## 📦 Установка

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/pebenifi/xeus.git
cd xeus
```

Или скачайте ZIP архив с GitHub и распакуйте его.

### Шаг 2: Создание виртуального окружения

**macOS/Linux:**
```bash
python3 -m venv venv
source venv/bin/activate
```

**Windows:**
```cmd
python -m venv venv
venv\Scripts\activate
```

> **Примечание:** Если команда `python3` не работает, попробуйте `python`. Убедитесь, что Python версии 3.8 или выше установлен.

### Шаг 3: Установка зависимостей

```bash
pip install --upgrade pip
pip install -r requirements.txt
```

Это установит:
- `PySide6` - графический фреймворк Qt для Python
- `pymodbus` - библиотека для работы с Modbus протоколом

### Шаг 4: Проверка установки

```bash
python -c "import PySide6; import pymodbus; print('Все зависимости установлены успешно!')"
```

Если команда выполнилась без ошибок, установка прошла успешно.

## ▶️ Запуск приложения

### Режим разработки (из исходного кода)

После установки зависимостей запустите приложение:

```bash
python main.py
```

Приложение откроется в новом окне. Вы должны увидеть графический интерфейс с различными элементами управления.

### Запуск собранного приложения

**macOS:**

1. Найдите файл `XeusGUI.app` в папке `dist/`
2. Двойной клик на `XeusGUI.app`
3. Если macOS показывает предупреждение о безопасности:
   - Перейдите в **Системные настройки** → **Безопасность и конфиденциальность**
   - Нажмите **"Открыть в любом случае"** рядом с предупреждением

**Windows:**

1. Найдите папку `dist\XeusGUI\`
2. Запустите `XeusGUI.exe`
3. Если Windows Defender показывает предупреждение:
   - Нажмите **"Дополнительные сведения"**
   - Выберите **"Выполнить в любом случае"**

### Подключение к Modbus устройству

1. Подключите Modbus устройство через последовательный порт (COM порт)
2. В приложении выберите соответствующий порт из списка
3. Нажмите кнопку **"Connect"**
4. После успешного подключения статус изменится на **"Connected"**

## 🔨 Сборка приложения

### Сборка для macOS

#### Автоматическая сборка (рекомендуется)

```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate

# Установите PyInstaller (если еще не установлен)
pip install pyinstaller

# Запустите скрипт сборки
./build_app.sh
```

#### Ручная сборка

```bash
# Активируйте виртуальное окружение
source venv/bin/activate

# Установите PyInstaller
pip install pyinstaller

# Запустите сборку
pyinstaller XeusGUI.spec --clean --noconfirm
```

После успешной сборки приложение будет находиться в `dist/XeusGUI.app`

#### Запуск собранного приложения

```bash
open dist/XeusGUI.app
```

### Сборка для Windows

#### Вариант 1: Локальная сборка на Windows

1. Откройте командную строку (cmd) или PowerShell
2. Перейдите в папку проекта:
   ```cmd
   cd путь\к\проекту\xeus
   ```
3. Создайте и активируйте виртуальное окружение:
   ```cmd
   python -m venv venv
   venv\Scripts\activate
   ```
4. Установите зависимости:
   ```cmd
   pip install -r requirements.txt
   pip install pyinstaller
   ```
5. Запустите сборку:
   ```cmd
   build_windows.bat onedir
   ```
   Или для одного файла:
   ```cmd
   build_windows.bat onefile
   ```

#### Вариант 2: Автоматическая сборка через GitHub Actions

1. Загрузите код в репозиторий:
   ```bash
   git add .
   git commit -m "Your commit message"
   git push
   ```
2. Перейдите на страницу репозитория на GitHub
3. Откройте вкладку **Actions**
4. Дождитесь завершения workflow "Build Windows"
5. Скачайте артефакты:
   - `XeusGUI-Windows-onedir.zip` - папка с приложением (рекомендуется)
   - `XeusGUI-Windows-onefile.zip` - один исполняемый файл

Подробнее см. [BUILD_WITH_GITHUB.md](BUILD_WITH_GITHUB.md)

#### Результаты сборки

**onedir (рекомендуется):**
- Создает папку `dist\XeusGUI\` с `XeusGUI.exe` и всеми библиотеками
- Быстрый запуск
- Легче отлаживать

**onefile:**
- Создает один файл `dist\XeusGUI.exe`
- Удобно для распространения
- Медленнее запуск (распаковка во временную папку)

## 📁 Структура проекта

```
xeus/
├── main.py                 # Точка входа приложения
├── modbus_manager.py       # Менеджер Modbus соединений и логики
├── modbus_client.py        # Клиент для работы с Modbus протоколом
├── app.qml                 # Главный QML файл приложения
├── Screen01.qml            # Основной экран интерфейса
├── Clinicalmode.qml        # Дополнительный экран
├── Constants.qml           # Константы для QML
├── qmldir                  # QML модуль директория
├── requirements.txt        # Python зависимости
├── XeusGUI.spec            # Конфигурация PyInstaller для macOS
├── XeusGUI_windows.spec     # Конфигурация PyInstaller для Windows (onedir)
├── XeusGUI_windows_onefile.spec  # Конфигурация PyInstaller для Windows (onefile)
├── build_app.sh            # Скрипт сборки для macOS
├── build_windows.bat       # Скрипт сборки для Windows
├── .github/
│   └── workflows/
│       └── build-windows.yml  # GitHub Actions workflow для автоматической сборки
├── BUILD_WINDOWS.md        # Подробная инструкция по сборке для Windows
├── BUILD_WITH_GITHUB.md    # Инструкция по использованию GitHub Actions
├── README_BUILD.md         # Краткая инструкция по сборке
└── README.md               # Этот файл
```

### Основные файлы

- **main.py** - Инициализирует Qt приложение, загружает QML интерфейс и создает ModbusManager
- **modbus_manager.py** - Управляет Modbus соединениями, обрабатывает команды и обновляет состояние устройств
- **modbus_client.py** - Низкоуровневая работа с Modbus протоколом (чтение/запись регистров)
- **app.qml** - Корневой QML файл, определяет структуру приложения
- **Screen01.qml** - Основной экран с элементами управления

## 🐛 Устранение проблем

### Проблема: "ModuleNotFoundError: No module named 'PySide6'"

**Решение:**
```bash
# Убедитесь, что виртуальное окружение активировано
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate  # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Проблема: "Qt platform plugin not found"

**Решение:**
- Убедитесь, что PySide6 установлен корректно: `pip install --upgrade PySide6`
- Проверьте, что все Qt плагины включены в сборку (должно работать автоматически)

### Проблема: QML файлы не загружаются

**Решение:**
- Убедитесь, что все QML файлы (`app.qml`, `Screen01.qml`, `Clinicalmode.qml`, `Constants.qml`) находятся в корне проекта
- Проверьте, что файл `qmldir` присутствует
- В режиме разработки файлы должны быть в той же папке, что и `main.py`

### Проблема: Приложение не запускается на macOS

**Решение:**
1. Проверьте, что Python 3.8+ установлен: `python3 --version`
2. Убедитесь, что виртуальное окружение активировано
3. Попробуйте запустить с явным указанием Python:
   ```bash
   /usr/bin/python3 main.py
   ```
4. Если приложение собрано, проверьте права доступа:
   ```bash
   chmod +x dist/XeusGUI.app/Contents/MacOS/XeusGUI
   ```

### Проблема: Modbus устройство не подключается

**Решение:**
1. Проверьте, что устройство подключено к правильному COM порту
2. Убедитесь, что порт не используется другим приложением
3. Проверьте настройки порта (скорость, биты данных, стоп-биты, четность)
4. На macOS может потребоваться разрешение на доступ к последовательным портам

### Проблема: Большой размер собранного приложения

**Решение:**
- Это нормально для приложений с Qt/PySide6
- Размер обычно составляет 200-500 MB из-за включения всех Qt библиотек
- Можно попробовать исключить неиспользуемые модули в `.spec` файле

### Проблема: Сборка не завершается или падает с ошибкой

**Решение:**
1. Убедитесь, что все зависимости установлены
2. Проверьте версию PyInstaller: `pip install --upgrade pyinstaller`
3. Очистите предыдущие сборки:
   ```bash
   rm -rf build dist  # macOS/Linux
   rmdir /s /q build dist  # Windows
   ```
4. Попробуйте собрать заново

### Проблема: Windows Defender блокирует приложение

**Решение:**
1. Это нормально для неподписанных приложений
2. Нажмите **"Дополнительные сведения"** → **"Выполнить в любом случае"**
3. Для распространения рекомендуется кодовая подпись приложения

## 💻 Разработка

### Настройка окружения разработки

1. Клонируйте репозиторий
2. Создайте виртуальное окружение (см. [Установка](#установка))
3. Установите зависимости для разработки:
   ```bash
   pip install -r requirements.txt
   ```

### Запуск в режиме разработки

```bash
python main.py
```

Изменения в Python коде требуют перезапуска приложения. Изменения в QML файлах могут применяться автоматически при перезагрузке (зависит от настроек Qt).

### Отладка

Приложение выводит логи в консоль. Для более детального логирования измените уровень в `main.py`:

```python
logging.basicConfig(
    level=logging.DEBUG,  # Измените с INFO на DEBUG
    ...
)
```

### Тестирование Modbus соединения

Используйте файл `test_modbus.py` для тестирования Modbus соединения:

```bash
python test_modbus.py
```

### Структура кода

- **modbus_manager.py** - содержит класс `ModbusManager`, который наследуется от `QObject` и предоставляет свойства и методы для QML
- **modbus_client.py** - содержит низкоуровневые функции для работы с Modbus протоколом
- QML файлы используют привязку данных (data binding) для автоматического обновления интерфейса при изменении свойств в Python

### Добавление новых функций

1. Добавьте новые свойства/методы в `ModbusManager` в `modbus_manager.py`
2. Используйте декораторы `@Property` и `@Slot` для экспорта в QML
3. Обновите QML интерфейс в `Screen01.qml` или других QML файлах
4. Протестируйте изменения в режиме разработки

## 📝 Лицензия

[Укажите лицензию, если применимо]

## 👥 Авторы

[Укажите авторов проекта]

## 🙏 Благодарности

- PySide6/Qt - за отличный фреймворк для создания GUI
- pymodbus - за библиотеку для работы с Modbus
- PyInstaller - за инструмент для сборки приложений

## 📞 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте раздел [Устранение проблем](#устранение-проблем)
2. Создайте [Issue](https://github.com/pebenifi/xeus/issues) на GitHub
3. Проверьте [документацию](https://github.com/pebenifi/xeus/wiki)

---

**Приятного использования XeusGUI!** 🚀

